

.encoding
    .image-upload-container(ondrop="dropImage(event)" ondragover="dragImageOver(event)")
        img(class="image before" max-width="200px" src="images/UploadIcon.svg")
        input(id="inputImage" name="inputImage" type="file" onchange="imageInputFromButton()" accept="image/*")
    .message-container
        .message-input
            label(for="messageInput") Input message 
            textarea#messageInput(name='messageInput' rows='4' cols='50')
            span#wordCount 0
            |  Characters

        //Encode Select
        .encode-input
            label(for="encodingMethod") Select Encoding Method 
            select#encodingMethod(name="encodingMethod")
                option(value="Option1") Option1 
                option(value="Option2") Option2
        button.button(type="submit") Encode Image
